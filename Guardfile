# Reload browser on js/css changes (see https://github.com/guard/guard-livereload)
guard 'livereload' do
  watch(%r{web-app/.+\.(css|js|html)})
  watch(%r{grails-app/views/.+\.(gsp)})
  watch(%r{grails-app/config/.+\.(groovy)})
  watch(%r{src/test/javascript/.+\.(js)})
end
